"""
Web Retrieval Layer -- optional plug-in package for Agent 3.

Deleting this entire `retrieval/` package is safe: agent3_discovery.py
imports it defensively (try/except ImportError) and falls back to
running exactly as it did before this package existed.
"""
